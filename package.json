{
  "name": "postgres_aws_rds_lambda",
  "version": "1.0.0",
  "description": "This is to test connection of postgres with rds and calling from lambda.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sagar Shah",
  "license": "ISC",
  "dependencies": {
    "pg": "^7.7.1",
    "pg-hstore": "^2.3.2",
    "sequelize": "^4.42.0"
  }
}
